zookeeperUrls: localhost:2181
statsDHost: localhost
statsDPort: 8125
statsDPrefix: kafka-monitoring
refreshSeconds: 60
pushToStatsD: false

server:
  applicationConnectors:
    - type: http
      port: 9090
      maxRequestHeaderSize: 32KiB
      maxResponseHeaderSize: 32KiB
